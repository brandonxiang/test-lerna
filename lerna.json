{
  "packages": [
    "packages/*"
  ],
  "version": "0.0.2",
  "npmClient": "npm",
  "command": {
    "publish": {
      "packages": "packages/base-*",
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish"
    }
  }
}
